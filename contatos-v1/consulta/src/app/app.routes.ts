import { Routes } from '@angular/router';
import { ListaContatosComponent } from './pages/lista-component/lista-component.component';

export const routes: Routes = [
    {
        path: '',
        component: ListaContatosComponent
    }
];
